# Prototype model (~23M params untied): pipeline validation + fast iteration.
# Untied embed/unembed (TransformerLens convention): params ~= 12*d^2*L + 2*V*d
#   blocks: 12 * 384^2 * 6 = 10.6M ; embeddings: 2 * 16384 * 384 = 12.6M
model:
  n_layers: 6
  d_model: 384
  n_heads: 6
  d_head: 64
  n_ctx: 512
  d_vocab: 16384
  act_fn: gelu
  normalization_type: RMS
  positional_embedding_type: rotary
train:
  seed: 1234
  lr: 6.0e-4
  betas: [0.9, 0.95]
  weight_decay: 0.1
  warmup_steps: 400
  max_steps: 15000           # ~0.49B tokens at 32*2*512 = 32,768 tok/step
  micro_batch_size: 32
  grad_accum: 2
  grad_clip: 1.0
  eval_every: 500
  ckpt_every: 1000
data:
  train_bin: data/full/train.bin
  val_bins:
    en: data/full/val_en.bin
    sw: data/full/val_sw.bin
    cs: data/full/val_cs.bin
out_dir: checkpoints/prototype_23m
